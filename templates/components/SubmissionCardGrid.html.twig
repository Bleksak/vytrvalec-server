<section {{ attributes }} class="profile-submissions">
    {% for submission in submissions %}
        <twig:SubmissionCard key="submission-{{ submission.id }}-{{ submission.updatedAt.timestamp }}" :submission="submission" />
    {% endfor %}
</section>
