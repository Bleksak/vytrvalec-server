{{ form_start(login) }}
<fieldset>
    {{ form_label(login.email) }}
    {{ form_widget(login.email) }}
    {{ form_errors(login.email) }}

    {{ form_label(login.password) }}
    {{ form_widget(login.password) }}
    {{ form_errors(login.password) }}

    {{ form_widget(login.submit) }}
</fieldset>
{{ form_end(login) }}
