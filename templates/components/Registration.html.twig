{{ form_start(registration) }}

<fieldset>
    {{ form_label(registration.faculty) }}
    {{ form_widget(registration.faculty) }}
    {{ form_errors(registration.faculty) }}
</fieldset>

<fieldset>
    {{ form_label(registration.email) }}
    {{ form_widget(registration.email) }}
    {{ form_errors(registration.email) }}
</fieldset>

<fieldset>
    {{ form_widget(registration.password) }}
    {{ form_errors(registration.password) }}
    {# TODO(@bleksak): Tady udelat PasswordProgress #}
</fieldset>

<fieldset>
    {{ form_label(registration.first_name) }}
    {{ form_widget(registration.first_name) }}
    {{ form_errors(registration.first_name) }}
</fieldset>

<fieldset>
    {{ form_label(registration.last_name) }}
    {{ form_widget(registration.last_name) }}
    {{ form_errors(registration.last_name) }}
</fieldset>

<fieldset>
    {{ form_widget(registration.anonymize) }}
    {{ form_label(registration.anonymize) }}
    {{ form_errors(registration.anonymize) }}
</fieldset>

<fieldset>
    {{ form_widget(registration.gdpr) }}
    {{ form_label(registration.gdpr) }}
    {{ form_errors(registration.gdpr) }}
</fieldset>

<fieldset>
    {{ form_widget(registration.submit) }}
</fieldset>

{{ form_end(registration) }}
